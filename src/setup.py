
"""
To install go to the folder with setup.py and run
`$ pip install -e .`

Then you can
`>>> import bogo`
"""

from setuptools import setup, find_packages

setup(name='bogo', version='0.1', packages=find_packages())
